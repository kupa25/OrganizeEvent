<Page
    x:Class="EventOrganize.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EventOrganize"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button x:Name="btnAddEvent" Content="Add Event" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="136,452,0,0" Click="btnTest_Click"/>
        <ListView x:Name="ListItems" HorizontalAlignment="Left" Height="421" VerticalAlignment="Top" Width="390">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Name="CheckBoxComplete" IsChecked="{Binding Complete, Mode=TwoWay}"  Click="CheckBoxComplete_Checked"  Content="{Binding Name}" Margin="10,5" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="btnJoinEvent" Content="Join Event" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="136,520,0,0" IsEnabled="False" Click="btnJoinEvent_Click"/>

    </Grid>
</Page>